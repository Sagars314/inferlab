% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_completeness.R
\name{basu_theorem_demo}
\alias{basu_theorem_demo}
\title{Basu's Theorem Demonstration}
\usage{
basu_theorem_demo(rfn, suff_fn, anc_fn, theta = 1, n = 20, B = 2000)
}
\arguments{
\item{rfn}{Function rfn(n, theta): generates a sample}

\item{suff_fn}{Function: computes complete sufficient statistic}

\item{anc_fn}{Function: computes proposed ancillary statistic}

\item{theta}{Reference theta value}

\item{n}{Sample size}

\item{B}{Monte Carlo replications}
}
\value{
List with correlation and independence test
}
\description{
Demonstrates Basu's theorem: if T is a complete sufficient statistic and
A is an ancillary statistic, then T and A are independent.
}
\details{
Tests empirical independence between a complete sufficient statistic and
a proposed ancillary statistic via Monte Carlo simulation.
}
\examples{
# Normal(mu, 1): X_bar is complete sufficient, Range is ancillary
rfn <- function(n, theta) rnorm(n, mean = theta, sd = 1)
suff_fn <- function(x) mean(x)              # complete sufficient
anc_fn  <- function(x) diff(range(x))        # ancillary
basu_theorem_demo(rfn, suff_fn, anc_fn, theta = 2, n = 10, B = 2000)
}
