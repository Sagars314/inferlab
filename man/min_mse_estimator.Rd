% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_estimation_methods.R
\name{min_mse_estimator}
\alias{min_mse_estimator}
\title{Minimum Mean Square Error Estimator}
\usage{
min_mse_estimator(
  x,
  family = c("normal_mean", "normal_var", "poisson", "exponential"),
  theta_prior = NULL,
  estimand = NULL
)
}
\arguments{
\item{x}{Numeric vector of observations}

\item{family}{Character: distribution family}

\item{theta_prior}{Numeric: prior estimate or true value of theta (for c* computation)}

\item{estimand}{Character: parameter being estimated}
}
\value{
List with MinMSE estimator and comparison to UMVUE
}
\description{
Finds the estimator of the form c * T(X) that minimizes the MSE,
where T is a known unbiased estimator. The optimal constant is:
c* = E[theta * T] / E[T^2]
}
\details{
For estimators of the form c * T where T is unbiased for theta:
MSE(cT) = c^2 * Var(T) + (c-1)^2 * theta^2
Minimized at c* = theta^2 / (theta^2 + Var(T))
}
\examples{
x <- rnorm(20, mean = 3, sd = 1)
min_mse_estimator(x, family = "normal_mean", theta_prior = 3)

x <- rexp(20, rate = 2)
min_mse_estimator(x, family = "exponential", theta_prior = 2)
}
