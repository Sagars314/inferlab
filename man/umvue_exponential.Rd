% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_umvue.R
\name{umvue_exponential}
\alias{umvue_exponential}
\title{UMVUE for Exponential Distribution}
\usage{
umvue_exponential(
  x,
  estimand = c("mean", "rate", "survival", "cdf"),
  t0 = NULL
)
}
\arguments{
\item{x}{Numeric vector of observations}

\item{estimand}{Character: "mean", "rate", "survival" or "cdf"}

\item{t0}{Numeric: value for survival/CDF estimation (required for those)}
}
\value{
List with UMVUE estimate
}
\description{
For X_i ~ Exp(lambda) (rate parameterization), the UMVUE of:
  - lambda: does not have a simple form via T = sum(X)
  - 1/lambda (mean): T/n = X_bar
  - P(X > t): (1 - t/T)^{n-1} for T > t
}
\examples{
x <- rexp(20, rate = 2)
umvue_exponential(x, estimand = "mean")
umvue_exponential(x, estimand = "survival", t0 = 0.3)
}
