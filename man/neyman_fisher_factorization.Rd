% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_sufficiency.R
\name{neyman_fisher_factorization}
\alias{neyman_fisher_factorization}
\title{Sufficient Statistic Verification via Neyman-Fisher Factorization}
\usage{
neyman_fisher_factorization(
  data,
  stat_fn,
  density_fn,
  theta_vals = c(1, 2, 3),
  n_check = 5
)
}
\arguments{
\item{data}{Numeric vector of observations}

\item{stat_fn}{Function computing the proposed sufficient statistic T(x)}

\item{density_fn}{Function f(x, theta) giving the joint density/pmf}

\item{theta_vals}{Numeric vector of theta values to test factorization}

\item{n_check}{Integer: number of data permutations to verify invariance}
}
\value{
List with factorization check results
}
\description{
Checks whether a proposed statistic T(x) is sufficient for theta by verifying
the Neyman-Fisher factorization: f(x; theta) = g(T(x), theta) * h(x).
}
\examples{
# For Poisson, T = sum(X) is sufficient for lambda
x <- rpois(10, lambda = 2)
stat_fn <- function(x) sum(x)
density_fn <- function(x, theta) prod(dpois(x, lambda = theta))
result <- neyman_fisher_factorization(x, stat_fn, density_fn, theta_vals = c(1,2,3))
}
