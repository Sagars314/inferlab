suff_stat <- function(model){
  cat("Compute sufficient statistic for parameter θ\n")
  cat("Using Neyman-Fisher Factorization Criterion\n")
}

minimal_suff_stat <- function(model){
  cat("Find minimal sufficient statistic\n")
  cat("Goal: smallest statistic preserving all parameter information\n")
}

ancillary_stat <- function(model){
  cat("Find ancillary statistic\n")
  cat("Statistic whose distribution does not depend on parameter θ\n")
}
